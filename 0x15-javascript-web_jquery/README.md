Here's a `README.md` file for your project:

```markdown
# JavaScript - Web jQuery

This project contains a series of tasks designed to practice JavaScript with jQuery. The tasks cover various aspects of DOM manipulation, event handling, and AJAX requests.

## Tasks

### Task 0
Write a JavaScript script that updates the text color of the `<header>` element to red (#FF0000).

**Requirements:**
- Use `document.querySelector` to select the HTML tag.
- Do not use the jQuery API.

**File:** `0-script.js`

**HTML File for Testing:** `0-main.html`

### Task 1
Write a JavaScript script that updates the text color of the `<header>` element to red (#FF0000) using the jQuery API.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `1-script.js`

**HTML File for Testing:** `1-main.html`

### Task 2
Write a JavaScript script that updates the text color of the `<header>` element to red (#FF0000) when the user clicks on the tag `<DIV#red_header>`.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `2-script.js`

**HTML File for Testing:** `2-main.html`

### Task 3
Write a JavaScript script that adds the class `red` to the `<header>` element when the user clicks on the tag `<DIV#red_header>`.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `3-script.js`

**HTML File for Testing:** `3-main.html`

### Task 4
Write a JavaScript script that toggles the class of the `<header>` element between `red` and `green` when the user clicks on the tag `<DIV#toggle_header>`.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `4-script.js`

**HTML File for Testing:** `4-main.html`

### Task 5
Write a JavaScript script that adds a `<li>` element to a list when the user clicks on the tag `<DIV#add_item>`.

**Requirements:**
- The new element must be: `<li>Item</li>`.
- The new element must be added to `UL.my_list`.
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `5-script.js`

**HTML File for Testing:** `5-main.html`

### Task 6
Write a JavaScript script that updates the text of the `<header>` element to "New Header!!!" when the user clicks on `<DIV#update_header>`.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `6-script.js`

**HTML File for Testing:** `6-main.html`

### Task 7
Write a JavaScript script that fetches the character name from the URL `https://swapi-api.alx-tools.com/api/people/5/?format=json` and displays it in the HTML tag `<DIV#character>`.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `7-script.js`

**HTML File for Testing:** `7-main.html`

### Task 8
Write a JavaScript script that fetches and lists the titles for all movies using the URL `https://swapi-api.alx-tools.com/api/films/?format=json`.

**Requirements:**
- All movie titles must be listed in the HTML tag `<UL#list_movies>`.
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.

**File:** `8-script.js`

**HTML File for Testing:** `8-main.html`

### Task 9
Write a JavaScript script that fetches the translation for “Hello” from `https://hellosalut.stefanbohacek.dev/?lang=fr` and displays it in the HTML tag `<DIV#hello>`.

**Requirements:**
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.
- The script must work when imported from the `<head>` tag.

**File:** `9-script.js`

**HTML File for Testing:** `9-main.html`

### Task 10
Write a JavaScript script that updates the text color of the `<header>` element to red (#FF0000) using `document.querySelector`.

**Requirements:**
- Do not use the jQuery API.
- The script must be imported from the `<head>` tag.

**File:** `100-script.js`

**HTML File for Testing:** `100-main.html`

### Task 11
Write a JavaScript script that adds, removes, and clears `<li>` elements from a list based on user clicks.

**Requirements:**
- When the user clicks on `<DIV#add_item>`, a new `<li>Item</li>` is added.
- When the user clicks on `<DIV#remove_item>`, the last `<li>` is removed.
- When the user clicks on `<DIV#clear_list>`, all `<li>` elements are removed.
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.
- The script must work when imported from the `<head>` tag.

**File:** `101-script.js`

**HTML File for Testing:** `101-main.html`

### Task 12
Write a JavaScript script that fetches and displays the translation of “Hello” based on the language code entered in `<INPUT#language_code>` when the user clicks on `<INPUT#btn_translate>`.

**Requirements:**
- Use the API service: `https://www.fourtonfish.com/hellosalut/hello/`.
- The translation must be displayed in `<DIV#hello>`.
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.
- The script must work when imported from the `<head>` tag.

**File:** `102-script.js`

**HTML File for Testing:** `102-main.html`

### Task 13
Write a JavaScript script that fetches and displays the translation of “Hello” based on the language code entered in `<INPUT#language_code>`. The translation is fetched when the user clicks on `<INPUT#btn_translate>` or presses ENTER in `<INPUT#language_code>`.

**Requirements:**
- Use the API service: `https://www.fourtonfish.com/hellosalut/hello/`.
- The translation must be displayed in `<DIV#hello>`.
- Do not use `document.querySelector` to select the HTML tag.
- Use the jQuery API.
- The script must work when imported from the `<head>` tag.

**File:** `103-script.js`

**HTML File for Testing:** `103-main.html`

## Setup

1. Clone the repository:
    ```bash
    git clone https://github.com/yourusername/alx-higher_level_programming.git
    ```

2. Navigate to the directory:
    ```bash
    cd alx-higher_level_programming/0x15-javascript-web_jquery
    ```

3. Open the `*.html` files in a browser to test the scripts.

## Contributing

Feel free to fork the repository and submit pull requests with improvements or fixes.

## License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.
```

Replace `"https://github.com/yourusername/alx-higher_level_programming.git"` with the actual URL of your GitHub repository if it's different.
